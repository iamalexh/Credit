#include <cs50.h>
#include <stdio.h>

int main(void)
{
int n;
    do
    {
        n = get_string("Enter the number of the credit card: ");
    }
    while (n < 13 || n > 16);
    
    if (n == 13)
    {
        n.14 * 2 + n.12 * 2 + n.10
    }

}
